# wechatDownload 剪贴板监听自动化 · 实验报告

> 实验日期：2026-08-15
> 结论：**剪贴板监听可被外部 Python 触发，business_id 可程序化切换** ✅

## 实验过程

| 步骤 | 操作 | 结果 |
|------|------|------|
| 1 | 启动 wechatDownload | ✅ PID 32744 |
| 2 | pywinauto 勾选「自动监听剪切板」(CheckBox_8) | ✅ 状态 0→1 |
| 3 | pywinauto 勾选「启动MCP」(CheckBox_14) | ✅ 端口 4545 通 |
| 4 | Set-Clipboard 设置北洋军乐团 URL | ✅ |
| 5 | 等待 5s，查 business_id | ✅ **切换为 MjM5NjI1NjM5Mg==（北洋军乐团）** |
| 6 | Set-Clipboard 设置天津大学 URL | ✅ |
| 7 | 等待 4s，查 business_id | ✅ **切换为 MjM5NzkwNzU0Mg==（天津大学）** |
| 8 | 调 batch_download_articles() | ✅ 返回 success |
| 9 | 尝试切回北洋军乐团 | ⚠️ 受「获取密钥」流程阻塞，状态不稳定 |

## 回答五个问题

### 1. 剪贴板监听能否被外部 Python 触发

**能。** 用普通 Windows 剪贴板 API（`Set-Clipboard` / `win32clipboard`）写入含 `__biz` 的公众号 URL 即可触发。工具会自动：

- 检测剪贴板变化（`QApplication.clipboard().dataChanged`）
- 提取 `__biz`，更新 `self.business_id`
- **自动将剪贴板改写为确认链接**（如 `...action=home&__biz=XXX&scene=124#wechat_redirect`），证明工具确实处理了 URL

### 2. business_id 是否发生变化

**是，可程序化切换。** 实测两次成功：

```
空 → 北洋军乐团 (MjM5NjI1NjM5Mg==)  ✅
北洋军乐团 → 天津大学 (MjM5NzkwNzU0Mg==)  ✅
```

### 3. MCP batch_download_articles() 是否随之切换公众号

**读取的是切换后的 business_id，逻辑上会跟随。** 但受「获取密钥」流程约束：

- 切换到一个**新公众号**后，工具自动触发「获取密钥」流程（日志出现 `开始获取密钥...`）
- 该流程可能需要**在微信打开确认链接**才能完成
- 若密钥未确认，batch 下载不开始（日志无「开始下载」记录），business_id 状态可能短暂不稳定

### 4. 最小可行自动化流程

```python
for url in 公众号URL列表:          # 每个号一篇已知文章链接
    set_clipboard(url)            # 写入剪贴板（含 __biz）
    sleep(3)                      # 等工具处理
    # 可选：检查剪贴板被改写为 confirmation URL 以确认处理完成
    mcp.batch_download_articles() # 批量下载当前 business_id
    wait_download_done()
```

**关键前提**：切换后的公众号密钥需已获取（或首次时微信人工确认一次）。密钥全局共享，已获取过的公众号切换后可直接下载。

### 5. 失败原因 / 限制

- **受密钥确认流程阻塞**：切换新公众号触发「获取密钥」，需微信确认；未确认前 batch 不下载，状态不稳定
- 其他公众号切换（密钥已有效时）应正常，但本次实验受天津大学密钥未确认影响，未能 100% 复现连续切换+下载

## 结论

**剪贴板监听是可行的程序化切换突破口，`business_id` 可自动更新。** 半自动方案可升级为：

```
外部 Python 依次写剪贴板公众号 URL → 工具自动切换 business_id
  → 调 MCP batch_download_articles() 下载
  → 仅首次/密钥过期时需微信人工确认
```

相比纯手动「切换+获取ID+批量下载」，压缩为「写剪贴板 + 调 MCP」，人工只负责密钥确认。

---

## 补充：collect.py 开发中的 flakiness 发现（2026-08-15 18:30）

**核心机制已在首次实验确凿验证**（剪贴板可触发、business_id 可切换）。

但开发 `collect.py` 时发现工具的重启稳定性问题：

### 现象
- 工具**重启后**，即使「自动监听剪切板」显示勾选（state=1，自动加载设置），剪贴板监听**可能不激活**
- 外部设置剪贴板 URL 后，剪贴板不被改写为确认链接，business_id 不切换
- 即使通过 UI 真实点击重新勾选（1→0→1），监听仍可能不恢复

### 推测原因
- 工具启动时若 setting 里已保存勾选状态（state=1），`setCheckState` 直接设置**不触发** `dataChanged` 监听绑定
- 首次实验成功是因为工具启动时监听为未勾选（0），真实点击 0→1 才绑定监听
- 一旦监听绑定失败，重启也难以恢复

### 对 collect.py 的影响
- `--check` 模式会检测「剪贴板监听」是否激活，未激活时提示人工处理
- **实际使用建议**：工具保持运行不要重启；若重启，需人工在 UI 里确认监听激活后再跑 collect.py
- 这是工具自身不稳定性，无法通过外部代码修复（不修改 wechatDownload 的前提下）
